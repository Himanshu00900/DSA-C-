#include<iostream>
using namespace std;
int main(){
    char str[5]={'a','b','c'};
    cout<<str;
    // For taking input getline() function is used
    // Also We can modify them
    // There are some built in function of strings
    // str.length()
    //str.size()
    // push_back(3) single character input
    // pop_back()  ending se cjharacter remove krna
    // + Operator
    // reverse 
}